# ==============================================
# MetalPro Backend - Environment Configuration
# ==============================================

# Node Environment
NODE_ENV=development
PORT=3001

# Database (PostgreSQL)
DATABASE_URL="postgresql://metalpro_user:metalpro_dev_password_2024@localhost:5432/metalpro?schema=public"

# Redis
REDIS_URL="redis://localhost:6379"

# JWT Authentication
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production-minimum-32-characters"
JWT_EXPIRES_IN="24h"
REFRESH_TOKEN_EXPIRES_IN="7d"

# Frontend URL (CORS)
FRONTEND_URL="http://localhost:8080"

# Backend URL (for file URLs)
BACKEND_URL="http://localhost:3001"

# File Upload Configuration
UPLOAD_DIR="uploads"

# ANAF API (existing)
ANAF_API_URL="https://webservicesp.anaf.ro/PlatitorTvaRest/api/v8/ws/tva"

# Email Service (AWS SES - Simple Email Service)
# For development: Leave AWS credentials empty (emails will be logged to console)
# For production:
#   1. Create AWS account and access IAM
#   2. Create IAM user with SES permissions (AmazonSESFullAccess)
#   3. Generate access keys for the IAM user
#   4. Verify sender email in SES console:
#      - Go to AWS Console → SES → Verified Identities
#      - Add and verify SES_FROM_EMAIL address
#      - Verify OPERATOR_EMAIL if you want to receive emails
#   5. Request production access (removes sandbox limits):
#      - SES Console → Account dashboard → Request production access
#   6. Set AWS_REGION to your preferred region (us-east-1, eu-west-1, etc.)
# AWS Credentials
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# Email Addresses
SES_FROM_EMAIL=noreply@metalpro.ro
OPERATOR_EMAIL=sales@metalpro.ro

# AWS S3 (File Storage) - To be configured later
# AWS_REGION=eu-central-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# S3_BUCKET_NAME=metalpro-uploads

# Monitoring (Sentry) - To be configured later
# SENTRY_DSN=

# Logging
LOG_LEVEL=info
